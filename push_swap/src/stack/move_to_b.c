#include "stack.h"
#include "../logic/logic.h"

int move_a_to_b(t_stack *a, t_stack *b) {
    int number_of_moves;
    int max_b_value;

    max_b_value = find_max_value(b);

    number_of_moves = 0;

    return number_of_moves;
}
